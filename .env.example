# Enhanced RAG System Environment Variables
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES (VITE_ prefix for client-side access)
# =============================================================================

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Application Configuration
VITE_APP_NAME="Enhanced RAG Education Platform"
VITE_APP_VERSION="1.0.0"
VITE_APP_ENVIRONMENT="development"

# File Upload Configuration
VITE_MAX_FILE_SIZE=52428800
VITE_ALLOWED_FILE_TYPES="application/pdf,text/plain,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document"
VITE_MAX_FILES_PER_UPLOAD=10

# Vector Search Configuration
VITE_DEFAULT_SIMILARITY_THRESHOLD=0.7
VITE_MAX_SEARCH_RESULTS=20
VITE_EMBEDDING_DIMENSION=1536
VITE_SEARCH_TIMEOUT=10000

# Chat Configuration
VITE_MAX_CHAT_HISTORY=100
VITE_CHAT_TIMEOUT=30000
VITE_MAX_MESSAGE_LENGTH=4000
VITE_STREAMING_ENABLED=true

# Performance Configuration
VITE_ENABLE_CACHING=true
VITE_CACHE_TTL=300000
VITE_LAZY_LOADING_ENABLED=true
VITE_VIRTUAL_SCROLLING_ENABLED=true

# Analytics Configuration
VITE_ANALYTICS_ENABLED=true
VITE_PRIVACY_MODE=true

# Security Configuration
VITE_RATE_LIMIT_ENABLED=true
VITE_MAX_REQUESTS_PER_MINUTE=60

# Development Configuration
VITE_DEBUG_MODE=false
VITE_LOG_LEVEL="info"
VITE_ENABLE_DEVTOOLS=false

# =============================================================================
# BACKEND/EDGE FUNCTION ENVIRONMENT VARIABLES (Server-side only)
# These should be set in Supabase Dashboard > Settings > Environment Variables
# =============================================================================

# AI Provider Configuration (DO NOT prefix with VITE_)
# OPENAI_API_KEY=your_openai_api_key_here
# CLAUDE_API_KEY=your_claude_api_key_here
# OPENAI_ORGANIZATION_ID=your_openai_org_id_here (optional)

# Supabase Service Configuration (DO NOT prefix with VITE_)
# SUPABASE_URL=your_supabase_url_here
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
# SUPABASE_JWT_SECRET=your_supabase_jwt_secret_here

# Database Configuration (DO NOT prefix with VITE_)
# DATABASE_URL=your_database_connection_string_here
# DB_POOL_SIZE=10
# DB_TIMEOUT=30000

# Redis Configuration (if using caching)
# REDIS_URL=your_redis_url_here
# REDIS_PASSWORD=your_redis_password_here

# Email Configuration (if using notifications)
# SMTP_HOST=your_smtp_host_here
# SMTP_PORT=587
# SMTP_USER=your_smtp_user_here
# SMTP_PASSWORD=your_smtp_password_here

# Monitoring and Logging
# SENTRY_DSN=your_sentry_dsn_here
# LOG_LEVEL=info
# ENABLE_METRICS=true

# =============================================================================
# NETLIFY ENVIRONMENT VARIABLES
# These should be set in Netlify Dashboard > Site Settings > Environment Variables
# =============================================================================

# Build Configuration
# NODE_VERSION=18
# NPM_FLAGS=--production=false
# BUILD_COMMAND=npm run build

# Deployment Configuration
# NETLIFY_SITE_ID=your_netlify_site_id_here
# NETLIFY_AUTH_TOKEN=your_netlify_auth_token_here

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Never commit actual API keys or secrets to version control
# 2. Use different keys for development, staging, and production
# 3. Rotate keys regularly
# 4. Use environment-specific configurations
# 5. Frontend variables (VITE_) are exposed to the client - never put secrets there
# 6. Backend variables should be set in Supabase Dashboard for Edge Functions
# 7. Netlify variables should be set in Netlify Dashboard for build process